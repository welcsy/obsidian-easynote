import { type FontConfig } from './index';

/** English font configuration */
export const enFontConfig: FontConfig = {
    body: '"Segoe UI", "Helvetica Neue", Arial, sans-serif',
    code: '"Cascadia Code", "Consolas", "Courier New", monospace',
};
